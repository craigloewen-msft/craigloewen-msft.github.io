---
import { Image } from 'astro:assets';

import BaseLayout from '@/layouts/BaseLayout.astro';
import Hero from '@/components/Hero.astro';
import SectionHeading from '@/components/SectionHeading.astro';
import StreamRow from '@/components/StreamRow.astro';
import ActionLink from '@/components/ActionLink.astro';
import Prompt from '@/components/Prompt.astro';
import VideoEmbed from '@/components/VideoEmbed.astro';
import { now, socials } from '@/lib/site';
import { getStream } from '@/lib/stream';

import konstanz from '@/assets/img/konstanz.jpg';
import climbing from '@/assets/img/climbing.jpg';
import guitarCover from '@/assets/img/guitar-cover.jpg';

const stream = await getStream();
const recent = stream.slice(0, 6);

const interests = [
  {
    title: 'Elsewhere',
    image: konstanz,
    alt: 'Konstanz, Germany',
    body: "I'm a South African Canadian that's lived in Africa, Europe and North America. I've picked up how to speak German, French and Korean along the way.",
  },
  {
    title: 'Training',
    image: climbing,
    alt: 'Craig climbing outdoors',
    body: 'Weight lifting and physical fitness are also big pillars in my life.',
  },
];
---

<BaseLayout path="/">
  <Hero />

  {/* Now */}
  <section class="mx-auto max-w-6xl px-5 pb-16 sm:px-8 sm:pb-20" data-reveal>
    <div class="surface-card p-8 sm:p-10">
      <Prompt label="now" />
      <ul class="mt-6 space-y-3.5">
        {now.items.map((item) => (
          <li class="flex gap-3 text-(--text-secondary)">
            <span
              aria-hidden="true"
              class="mt-2 size-1.5 shrink-0 rounded-full"
              style="background: var(--tone-accent)"
            />
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  {/* Writing & talks */}
  <section id="writing" class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-20" data-reveal>
    <SectionHeading
      kicker="writing & talks"
      title="My latest works"
      href="/writing"
      hrefLabel="See everything"
    />

    <ul class="mt-10 divide-y divide-(--surface-border) border-y border-(--surface-border)">
      {recent.map((item) => (
        <li>
          <StreamRow item={item} showDescription={false} />
        </li>
      ))}
    </ul>
  </section>

  {/* When I'm not working */}
  <section id="beyond" class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-20" data-reveal>
    <SectionHeading kicker="offline" title="When I'm not working" />

    <div class="mt-10 grid gap-5 md:grid-cols-3">
      <article class="surface-card overflow-hidden">
        <VideoEmbed
          id="9AbamDR33lc"
          title="Master of Puppets - Metallica (Guitar Cover)"
          poster={guitarCover}
          posterAlt="Craig playing an electric guitar in his living room"
          class="aspect-video border-b border-(--surface-border)"
        />
        <div class="p-6">
          <h3
            class="font-mono text-sm tracking-[0.14em] uppercase"
            style="color: var(--tone-accent)"
          >
            Playing
          </h3>
          <p class="mt-3 text-sm text-(--text-muted)">
            Guitar, loudly. Anything from Metallica to classical pieces.
          </p>
        </div>
      </article>

      {interests.map((interest) => (
        <article class="surface-card overflow-hidden">
          <div class="aspect-video overflow-hidden border-b border-(--surface-border)">
            <Image
              src={interest.image}
              alt={interest.alt}
              width={640}
              height={360}
              loading="lazy"
              class="size-full object-cover"
            />
          </div>
          <div class="p-6">
            <h3
              class="font-mono text-sm tracking-[0.14em] uppercase"
              style="color: var(--tone-accent)"
            >
              {interest.title}
            </h3>
            <p class="mt-3 text-sm text-(--text-muted)">{interest.body}</p>
          </div>
        </article>
      ))}
    </div>
  </section>

  {/* Contact */}
  <section id="contact" class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-24" data-reveal>
    <div class="surface-card grain relative overflow-hidden p-10 text-center sm:p-16">
      <div
        class="pointer-events-none absolute inset-x-0 -top-24 h-48 blur-[100px]"
        style="background: var(--glow-accent)"
        aria-hidden="true"
      ></div>

      <Prompt label="say hello" cursor />
      <h2 class="mt-4 text-3xl font-semibold sm:text-4xl">Get in touch</h2>
      <p class="mx-auto mt-4 max-w-lg text-(--text-muted)">
        Speaking invitations, running open source inside a big company, or where developer tools go
        next — I'm glad to talk about any of it.
      </p>

      <div class="mt-8 flex flex-wrap justify-center gap-3">
        {socials.map((s) => (
          <ActionLink
            href={s.url}
            variant={s.icon === 'mail' ? 'primary' : 'ghost'}
            icon={s.icon}
            analytics={`contact_${s.icon}`}
            external={s.icon !== 'mail'}
          >
            {s.label}
          </ActionLink>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
